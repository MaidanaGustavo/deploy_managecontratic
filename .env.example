# ===================================
# DESENVOLVIMENTO LOCAL
# ===================================

# URL da API Next.js (Backend)
VITE_API_URL=http://localhost:3001/api

# URL do Frontend (para CORS)
FRONTEND_URL=http://localhost:5173

# MongoDB (Local)
MONGODB_URI=mongodb://localhost:27017/contractic
MONGODB_URI_TEST=mongodb://localhost:27017/contractic-test

# ===================================
# PRODUÇÃO (RAILWAY)
# ===================================

# Railway automaticamente fornece DATABASE_URL ou MONGO_URL quando você adiciona um MongoDB
# Se usar MongoDB Atlas ou serviço externo, defina manualmente:
# DATABASE_URL=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# VITE_API_URL - Em produção, deixe vazio ou defina como /api para usar same-origin
# Isso faz com que o frontend chame /api (mesmo servidor) em vez de http://localhost:3001/api
# VITE_API_URL=/api

# PORT - Railway define automaticamente, mas você pode sobrescrever
# PORT=3000

# Node Environment - Railway define automaticamente como production
# NODE_ENV=production

# FRONTEND_URL - Não é necessário em produção pois frontend e backend rodam no mesmo servidor
# CORS é desabilitado automaticamente em produção
